---
name: ramchal-messages
description: >-
  מערכת ניהול ההודעות של בית כנסת הרמח"ל מיצד (ramchal-messages) — index.html על GitHub Pages
  + בקאנד Apps Script (Code.gs) ששולח זמני תפילות לשבת ל-metzad-chevra@googlegroups.com כל חמישי
  19:00. השתמש בסקיל בכל שאלה/משימה על: זמני תפילות/זמני שבת, חישוב מנחה/מעריב/שחרית/אבות
  ובנים/מוצ"ש, Hebcal API, elevation ו-i=on, שקיעה/צאת שבת/סוף זמן ק"ש, פרשת השבוע
  ו-translateParasha, sendWeeklyZmanim, getZmanimForShabbat, setupWeeklyTrigger, buildPlainMessage,
  buildHtmlEmail, buildHtmlEmailFromText, buildBlankPage והבלאנק (TOP_B64/BOT_B64),
  buildShabbatMessage, loadZmanim, fetchCustomZmanim, fillShabbatZmanim, doSend, doGet ו-action=
  send/setMode/zmanim/getData/saveHistory/saveTemplates, JSONP וגיליון RamchalSync, מצב test/prod,
  דילוג בשבוע שיש בו חג (isHolidayWeekend/notifyGabbai), תבניות, והיסטוריית שליחות.
  כולל גם **ניהול מתפללים וחובות** (טאב מתפללים, renderMembers, כרטיס חוב/ledger, חיוב מרוכז,
  memberBalance, membersUpsert, גיליונות members ו-ledger) ו**רישום לימים נוראים** (טאב ימים נוראים,
  renderHH, מקומות לר"ה ויו"כ לגברים ולעזרת נשים, דמי רישום, קיבולת, hhUpsert, גיליון hh),
  וכן hardReload/APP_VERSION. מופעל גם על "המייל של בית הכנסת", "הזמנים לא נכונים",
  "שנה את מנחה ל-25 דקות", "לא נשלח המייל השבועי", "הלוגו לא נראה במייל", "המערכת של הרמחל",
  "רשימת המתפללים", "כמה חוב יש ל...", "מי נרשם לראש השנה", "מקומות בעזרת נשים".
---

# בית כנסת הרמח"ל מיצד — מערכת ניהול הודעות 🕍

**תיעוד שנכתב מקריאה ישירה של הקוד החי. עודכן 18.8.2026 (מתפללים וחובות, ימים נוראים,
רענון עמוק, תיקון תאימות נייד).**

מערכת לניהול ושליחת הודעות לציבור המתפללים בבית כנסת הרמח"ל בישוב מיצד: חישוב זמני התפילות
לשבת ושליחתם במייל לרשימת התפוצה הישובית, הודעות חופשיות (חגים, אירועים, שיעורים, תזכורות),
**ניהול רשימת המתפללים ומעקב חובות**, ו**רישום מקומות לימים נוראים**.

## ארכיטקטורה

**Stack:** קובץ HTML יחיד — Vanilla JS בלבד (ללא React, ללא build, ללא תלויות מלבד פונט Heebo)
+ **Hebcal API** (זמנים, פרשה, חגים) + **Google Apps Script** (שליחת מייל, טריגר שבועי, אחסון)
+ **Google Sheets** (`RamchalSync`) + **localStorage**. התקשורת פרונט↔בקאנד: `fetch` עם
`mode:'no-cors'` לכתיבה, **JSONP** (הזרקת `<script>`) לקריאה — עקיפת CORS, כמו בשירת הציפורים.

| רכיב | מיקום | תפקיד |
|---|---|---|
| `index.html` | הריפו (~119KB, 3175 שורות) | ממשק הניהול — CSS + HTML + JS בקובץ אחד |
| `Code.gs` | הריפו (**עותק מראה**) + פרויקט Apps Script (הקוד החי) | שליחה אוטומטית, Web App, אחסון |
| `README.md` | הריפו | הוראות התקנה ל-GitHub Pages וחיבור ל-Apps Script |

**⚠ שתי דרכי שליחה שונות — ההבחנה הזו היא המפתח לכל תקלה:**

1. **אוטומטית** — טריגר `sendWeeklyZmanim` בימי חמישי 19:00. מחשב זמנים בעצמו, שולח **תמיד
   ל-`CONFIG.EMAIL_TO` (פרודקשן)**, ומדלג אם יש חג בסופ"ש. **לא מושפע כלל מהטוגל test/prod באתר.**
2. **ידנית מהאתר** — `action=send`. הפרונט מחשב את הזמנים ובונה את הטקסט, הבקאנד רק עוטף ושולח
   ליעד לפי `mode`.

הזמנים בשתי הדרכים **מחושבים בקוד נפרד** ולכן עלולים להיות שונים — ראו באג 1.

**מקורות אמת (לאמת מולם לפני כל שינוי — לא לסמוך על הזיכרון):**
- ריפו: `https://github.com/shirat-hatziporim/ramchal-messages` (ציבורי, `main`)
- קריאה ללא טוקן: `https://raw.githubusercontent.com/shirat-hatziporim/ramchal-messages/main/index.html`
- אתר חי: `https://shirat-hatziporim.github.io/ramchal-messages/`
- Webhook (מוטמע ב-`index.html` שורה ~1079):
  `https://script.google.com/macros/s/AKfycbwf0HAdQHgkmxHSoUAkrhuiyz_GL0h2ZyXfr1C7gs9ii6phXFQikbw2o_GxDaWzo69acA/exec`
- פרויקט Apps Script: `זמני שבת - הרמח"ל` (גרסה 2.1)
- גיליון סנכרון: `RamchalSync` בדרייב (מאותר לפי **שם** דרך `getFilesByName`, לא לפי ID)
- דוקומנט וואטסאפ: `הודעת וואטסאפ - זמני שבת` (נדרס בכל שליחה אוטומטית)
- תפוצה: `metzad-chevra@googlegroups.com` | בדיקה: `4103353@gmail.com`
- מיצד: `31.6219, 35.1469`, `Asia/Jerusalem`, גובה **900 מ'**

⚠ **`Code.gs` שבריפו הוא מראה בלבד — אין CI.** דחיפה לריפו **לא** מעדכנת את הסקריפט החי; יעקב
חייב להדביק ידנית בעורך Apps Script. וכשהוא עורך בעורך, הריפו מתיישן. לכן: לפני כל שינוי בבקאנד —
לשאול אם ערך בעורך מאז, ואחרי כל שינוי — לדחוף לריפו **וגם** למסור קובץ מלא להדבקה.

**לוח הזמנים של הישוב:** הזמנים אמורים לתאום ל**עתים לבינה, אופק מיצד**. הלוח עצמו אינו במערכת
ואינו בפרוייקט — יעקב אמר (13.8.2026) שיעלה מהדורה חדשה לשנה שלמה בסביבות ספטמבר 2026. סביר
ש-`elevation=900&ue=on` פחות דקה הוא **כיול** מול הלוח הזה (השערה מקריאת הקוד, לא ודאות). עד
שהלוח יגיע — **יעקב ביקש לא לשנות את חישוב הזמנים; לדבריו הזמנים הנשלחים נכונים.**

## ⭐ כללי עבודה קבועים

- **דחיפות ישירות ל-GitHub** — יעקב מעדיף שאדחוף דרך ה-API; הוא לעיתים עובד מנייד.
  לכן **כל פיצ'ר חדש חייב להיבדק מנטלית ברוחב 360px** לפני דחיפה (ראו תקלת הטאבים).
- **לעדכן `APP_VERSION` בכל דחיפה של `index.html`** — בלעדיו אי אפשר לדעת אם יעקב רואה
  את הגרסה החדשה או מטמון ישן, וזה בזבז שלוש סבבים ב-18.8.
- **תמיד למשוך את הגרסה האחרונה מ-raw לפני עריכה** — לא לערוך מתוך זיכרון של שיחה קודמת.
- **שינוי ב-`Code.gs` ⇒ למסור את הקובץ המלא המעודכן** להדבקה בעורך Apps Script (אין clasp/CI כאן).
  ⚠ הקובץ מכיל שני מחרוזות base64 ענקיות (`TOP_B64`, `BOT_B64`, ~50KB) — **לשמר אותן במדויק**.
- **בדיקת תחביר לפני מסירה:** לחלץ את ה-`<script>` / להעתיק ל-`.js` ולהריץ `node --check`.
- **מצב בדיקה קודם לפרודקשן** — בכל שינוי בתוכן ההודעה או בחישוב: לשלוח קודם ב-`test`. אבל לזכור
  שהטוגל **לא מגן על השליחה האוטומטית** — שינוי ב-`Code.gs` מגיע לכל הישוב ביום חמישי הקרוב.
  לבדיקה של הנתיב האוטומטי: להריץ `testRun()` בעורך אחרי שינוי זמני של `CONFIG.EMAIL_TO`.
- **זמנים הם הלכה, לא UI.** טעות של דקה במנחה או במוצ"ש היא תקלה אמיתית לציבור. כל נגיעה בחישוב —
  לאמת מול Hebcal ידנית לתאריך אחד לפחות ולהציג ליעקב את המספרים לפני/אחרי.

## מפת `index.html` (הפרונט)

```
1–640      <style> — משתני CSS (--navy #1a3a5c, --gold #c9a84c) + media query נייד
641–1420   HTML — header, 7 טאבים, 4 מודאלים
  647 #tab-dashboard | 737 #tab-shabbat | 826 #tab-custom | 968 #tab-history
  999 #tab-members  | 1073 #tab-hh      | 1147 #tab-settings
1426–3175  <script>
```

| שורה | פונקציה | תפקיד |
|---|---|---|
| 1428 | `STATE` | `{zmanim, history, members, ledger, hh, settings}` — localStorage + ברירות מחדל |
| 1446 | `APP_VERSION` / 1450 `hardReload()` | מספר גרסה + רענון עוקף-מטמון (ראו "רענון עמוק") |
| 1464 | `switchTab()` | מפעיל `renderMembers()` / `renderHH()` לפי הטאב |
| 1495 | `toMinutes()` / `shacharitNote()` | עוזרי חסם השחרית |
| 1519 | `loadZmanim()` | זמנים+פרשה מ-Hebcal לשבת שנבחרה ⚠ **ללא `elevation`** |
| 1616 | `buildShabbatMessage()` | **תבנית מייל השבת בצד הלקוח** — כאן משנים את הנוסח |
| 1691 | `doSend()` | `fetch` ל-webhook עם `action=send` + שמירה בהיסטוריה |
| 1830 | `loadTemplate()` | 6 תבניות: `chag`,`shabbat_chag`,`chag_shabbat`,`event`,`reminder`,`shiur` |
| 1927 | `toggleMode()` | test⇄prod — שומר מקומית ושולח `setMode` (שאינו עושה דבר, ראו באג 2) |
| 1967 | `fetchCustomZmanim()` | זמנים לטאב החופשי — **עם** `elevation=900&ue=on` ומינוס דקה |
| 2329 | `translateParasha()` | מילון פרשות אנגלית→עברית (עותק זהה קיים ב-`Code.gs`) |
| **2365–2802** | **בלוק מתפללים** | ראו "מתפללים וחובות" למטה |
| **2804–3068** | **בלוק ימים נוראים** | ראו "רישום לימים נוראים" למטה |
| 3096 | `loadSyncData()` | JSONP — מושך `history`+`templates` וממזג עם המקומי |

⚠ מספרי השורות נכונים ל-18.8.2026 (3175 שורות). הקובץ משתנה — לאמת מול `grep` לפני עריכה.

**localStorage:** `msg_history` (עד 100), `msg_settings`, `custom_templates`,
`shul_members`, `shul_ledger`, `shul_hh`, `hh_year`, `hh_capacity`.

**⚠ תאימות נייד (תוקן 18.8.2026):** `.nav-tabs-inner` הוא `flex` ללא גלישה. עם 7 טאבים הוא חרג
מרוחב המסך ומשך את **כל העמוד** הצידה (הכותרת נחתכה, גלילה אופקית). התיקון: `flex-wrap: wrap`
ב-media query של 700px, `white-space:nowrap` על `.tab-btn`, ו-`overflow-x:hidden` על `html,body`
כרשת ביטחון. **בהוספת טאב שמיני — לבדוק בנייד.**

## מפת `Code.gs` (הבקאנד)

| בלוק | תפקיד |
|---|---|
| `CONFIG` | יעד המייל, שם ביהכ"נ, קואורדינטות, **6 קבועי דקות** — מקור האמת לשליחה האוטומטית |
| `TOP_B64` / `BOT_B64` | הבלאנק הרשמי — JPEG בבסיס 64. TOP למעלה, BOT למטה, התוכן נמתח באמצע |
| `sendWeeklyZmanim()` | **נקודת הכניסה של הטריגר**: זמנים → פרשה → בדיקת חג → מייל → וואטסאפ |
| `getZmanimForShabbat()` | Hebcal עם `elevation=900&ue=on`, שקיעה **מינוס דקה**, ואז 6 החישובים |
| `buildPlainMessage()` | טקסט לוואטסאפ ולגוף ה-plain של המייל (עם `*הדגשות*`) |
| `buildHtmlEmail()` | מייל השבת האוטומטי — טבלה בנויה ידנית, שורה לכל זמן |
| `buildHtmlEmailFromText()` | ממיר טקסט חופשי מהאתר לטבלת HTML — ראו "שפת הטקסט" למטה |
| `buildBlankPage()` | עוטף כל תוכן בבלאנק (600px, RTL, רספונסיבי) |
| `isHolidayWeekend()` / `checkIfHoliday()` | האם שישי או ראשון הם יו"ט → דילוג |
| `notifyGabbai()` | מייל לגבאי: "יש לשלוח ידנית השבוע" |
| `getParasha()` | שיטה 1: `shabbat` endpoint; שיטה 2: `hebcal` לפי חודש; אחרת "השבוע" |
| `setupWeeklyTrigger()` | **מוחק טריגרים קיימים ויוצר חדש** — חמישי 19:00. להריץ ידנית פעם אחת |
| `testRun()` | מריץ את הזרימה המלאה מיד |
| `doGet(e)` | ה-Web App — כל ה-actions |
| `getOrCreateSyncSheet` / `syncGetData` / `syncSaveHistory` / `syncSaveTemplates` | הגיליון |
| `getMembersSheets()` (614) | יוצר/מחזיר את שלוש הלשוניות `members`, `ledger`, `hh` |
| `membersGetAll()` (653) | קורא את שלושתן ומחזיר `{members, ledger, hh}` |
| `membersUpsert` / `membersDelete` (695, 711) | upsert לפי `id`; מחיקת מתפלל מוחקת גם את תנועותיו |
| `ledgerUpsert` / `ledgerDelete` (726, 741) | תנועות חיוב/תשלום |
| `hhUpsert` / `hhDelete` (750, 766) | רישומי ימים נוראים |
| `hhSummary(year)` (776) | סיכום מקומות לשנה — לשימוש ידני בעורך |
| `memberBalanceGs(id)` (788) | יתרת חוב בצד השרת (לא בשימוש בפרונט) |
| `handleMembersAction(e, output)` (800) | **ראוטר** — נקרא ראשון ב-`doGet`, מחזיר `null` אם ה-action אינו שלו |
| `normalizeSheetDate()` (645) | Sheets ממיר `"YYYY-MM-DD"` ל-`Date` — זה מחזיר למחרוזת |
| `getHebrewDate()` | **קוד מת** — לא נקרא מאף מקום |

## חישוב הזמנים

מקור: `hebcal.com/zmanim?cfg=json&latitude=31.6219&longitude=35.1469&tzid=Asia/Jerusalem&date=<YYYY-MM-DD>&sec=1`
(בבקאנד ובטאב החופשי מתווסף `&elevation=900&ue=on`, והשקיעה מוחסרת בדקה).

| זמן | נוסחה | קבוע ב-`CONFIG` | הגדרה בפרונט |
|---|---|---|---|
| שקיעה | `times.sunset` (−1 דק') | — | — |
| צאת שבת | `times.tzeit85deg` (גיבוי `nightfall`) | — | — |
| סוף זמן ק"ש (גר"א) | `times.sofZmanShma` | — | — |
| מנחה ערב שבת | שקיעה −20 | `MINCHA_BEFORE_SHKIA_MIN` | `settings.mincha` |
| מעריב ליל שבת | שקיעה +35 | `MAARIV_AFTER_SHKIA_MIN` | `settings.maariv` |
| שחרית | סוף ק"ש −40, **ולא יאוחר מ-08:40** | `SHACHARIT_BEFORE_SOF_KRIA_MIN` + `SHACHARIT_LATEST` | `settings.shacharit` + `settings.shacharitLatest` |
| מנחה של שבת | שקיעה −40 | `MINCHA_SHABBAT_BEFORE_SHKIA_MIN` | `settings.minchaShab` |
| אבות ובנים | מנחת שבת −40 | `AVOT_BANIM_BEFORE_MINCHA_MIN` | ⚠ **מוטמע** (שורה 1160) |
| מעריב מוצ"ש | צאת שבת +10 | `MAARIV_MOTZASH_AFTER_TZET_MIN` | `settings.motzash` |

**תאריך היעד:** השבת הקרובה — `(6 - dow + 7) % 7 || 7`. ביום שבת עצמו יילקח **השבת הבא**.

**חסם שחרית (נוסף 13.8.2026):** ⚠ **סוף זמן ק"ש מאוחר בקיץ, לא בחורף** — היום ארוך, השעות
הזמניות ארוכות, ושלוש שעות מהנץ נופלות מאוחר. החסם פועל בערך באוגוסט–אוקטובר ובסוף מרץ–אפריל
(הקפיצות הן מעבר שעון קיץ/חורף), ולא פועל בנובמבר–פברואר ובאמצע הקיץ. −40 הפיק שעות כמו 8:42. עכשיו:
אם התוצאה מאוחרת מ-`08:40` — נלקח `08:40`, **וההערה "(40 דק' לפני סוף זמן ק"ש)" נעלמת** (היא כבר
לא נכונה; יעקב בחר במפורש בלי הערה חלופית). הדגל הוא `shacharitCapped` בשני הקבצים, והוא נבדק
ב-`renderZmanim`, `buildShabbatMessage`, `fillShabbatZmanim`, `buildPlainMessage`, `buildHtmlEmail`.
השעה ניתנת לשינוי: שדה `#setting-shacharit-latest` בהגדרות, ו-`CONFIG.SHACHARIT_LATEST` בבקאנד.

## חוזה ה-API (`doGet`)

| Action | פרמטרים | תפקיד |
|---|---|---|
| `send` | `mode`, `subject`, `body`, (`parasha` — **הפרונט לא שולח**) | עוטף בבלאנק ושולח לפי `mode` |
| `getData` | `callback` | **JSONP** — `{ok:true,data:{history:[],templates:[]}}` |
| `saveHistory` | `entry` (JSON) | `appendRow` לגיליון |
| `saveTemplates` | `templates` (JSON) | מוחק הכל וכותב מחדש |
| `test` | — | בדיקת חיבור |
| `setMode` | `mode` | **רק כותב ללוג — לא משנה כלום** |
| `zmanim` | `parasha` | ⚠ שולח את מייל השבת האוטומטי **לפרודקשן, מתעלם מ-`mode`**. הפרונט לא קורא לו |
| `getMembers` | `callback` | **JSONP** — `{ok:true,data:{members:[],ledger:[],hh:[]}}` |
| `saveMember` / `deleteMember` | `entry` (JSON) / `id` | upsert / מחיקה (כולל תנועות) |
| `saveLedger` / `deleteLedger` | `entry` (JSON) / `id` | תנועת חיוב או תשלום |
| `saveHH` / `deleteHH` | `entry` (JSON) / `id` | רישום לימים נוראים |

⚠ פעולות המתפללים **אינן** עושות `decodeURIComponent` (בניגוד ל-`send`/`saveHistory` — ראו באג 5).
זה הנכון; אין לחקות שם את הדפוס הישן.

**השלמת פרשה בשרת:** אם `parasha` ריק / `[פרשה]` / `השבוע` — הבקאנד שולף בעצמו מ-Hebcal
ומחליף גם בגוף וגם בנושא (כולל regex שמחליף שם פרשה **באנגלית** בנושא).

## שפת הטקסט של `buildHtmlEmailFromText`

זה מסביר למה כפתורי `insertSeparator` בפרונט מייצרים בדיוק את התחביר הזה:

| בטקסט | בפלט |
|---|---|
| `תווית: 14:30 (הערה)` | שורת טבלה: תווית + הערה קטנה מימין, שעה מודגשת משמאל |
| `=== יום א - חג ===` | כותרת יום ממורכזת בכחול |
| `~ טקסט ~` | תיבת הדגשה עם פס זהב |
| `---` | קו מפריד |
| שורה אחרת | טקסט נטוי ממורכז |

⚠ **הפונקציה מוחקת את כל השורות עד ה-`---` הראשון** — אם הוא מופיע בחמש השורות הראשונות.
לכן הכותרת ושם ביהכ"נ שבראש הודעת השבת נעלמים במייל (הנושא מוצג במקומם). **זו התנהגות מכוונת —
אל "לתקן" אותה בלי לשאול, אחרת הכותרת תופיע פעמיים.**

## מתפללים וחובות (נוסף 18.8.2026)

טאב `#tab-members`. **רשימת 31 המתפללים מוטמעת בקוד** כ-`SEED_MEMBERS` (2367) ונזרעת פעם אחת
ב-`seedMembersIfNeeded()` (2410) אם `STATE.members` ריק. **לא לזרוע מחדש בכל טעינה** — זה ידרוס
עריכות. הזריעה נותנת מזהים `m1`–`m31`; מתפלל חדש מקבל `'m' + Date.now()`.

**מודל הנתונים:**
```
member = {id, last, first, father, yichus:'כהן'|'לוי'|'', phone, email, address, notes, active}
ledger = {id, memberId, date:'YYYY-MM-DD', type:'charge'|'payment', amount, desc}
```
**יתרה = סכום החיובים פחות התשלומים** (`memberBalance`, 2439). חיובי = חוב (אדום), שלילי = זכות
(ירוק). אין שדה "יתרה" שמור — היא תמיד מחושבת מהתנועות, ולכן אין סכנת אי-עקביות.

| פונקציה | שורה | הערה |
|---|---|---|
| `renderMembers()` | 2459 | טבלה + 4 מדדים + חיפוש/סינון/מיון דרך `MFILTER` |
| `memberFullName()` | 2426 | **שם משפחה לפני שם פרטי** (שונה 18.8) — משמש גם ב-`hhRegName` וברשימה הנפתחת |
| `openLedger` / `renderLedger` / `addLedgerEntry` | 2593–2643 | כרטיס החוב |
| `applyBulkCharge()` | 2685 | חיוב מרוכז לכל `active !== false` |
| `exportMembersCSV()` | 2711 | עם BOM לעברית באקסל |
| `loadMembersSync()` | 2756 | JSONP; **השרת גובר**, רשומות מקומיות-בלבד נדחפות אליו |
| `pushAllMembers()` | 2746 | דחיפה סדרתית, 200ms בין רשומה לרשומה |

**⚠ תזמון הסנכרון:** כל כתיבה נשלחת מיד ב-`_mFetch` (`no-cors`, ראו באג 4 — אין אישור).
דחיפה מרובה מרווחת ב-200ms כדי לא להיחסם. בטעינה ראשונה שבה השרת ריק והמקומי מלא — נדחף הכל
אוטומטית ובשקט.

## רישום לימים נוראים (נוסף 18.8.2026)

טאב `#tab-hh`. רישום מקומות לראש השנה וליום כיפור, **בהפרדה בין גברים לעזרת נשים**.

```
reg = {id, year:'תשפ"ז', memberId, name, rhMen, rhWomen, ykMen, ykWomen, fee, chargeId, notes}
```

- **שנה** — `HH_YEARS` (2805) מכיל תשפ"ו–תש"צ. `hhDefaultYear()` (2808) מחשב לפי השנה הלועזית
  וחודש ≥ אוגוסט. ⚠ **המיפוי מוטמע וייגמר בתש"צ (2030)** — להאריך את המערך אז.
- כל שנה נפרדת; שינוי השנה בבורר לא נוגע ברישומי שנה אחרת. השנה הנבחרת ב-`hh_year`.
- `memberId` ריק ⇒ **אורח** ששמו ב-`name` (מתארחים שאינם ברשימת המתפללים).
- **קיבולת** (`hh_capacity`, גלובלית ולא פר-שנה) מציגה כמה מקומות נותרו. החישוב לוקח את
  ה**מקסימום** בין ר"ה ליו"כ, לא את הסכום — אותו כיסא משמש בשני המועדים.
- `chargeHHFee()` (3016) יוצר תנועת `charge` בכרטיס המתפלל עם הפירוט "מקומות ימים נוראים <שנה>",
  ושומר את `chargeId` ברישום כדי למנוע חיוב כפול. הכפתור מוסתר לאורחים (אין להם כרטיס).

## רענון עמוק (`hardReload`, 1450)

GitHub Pages ודפדפני נייד מגישים את `index.html` ממטמון, ויעקב ראה שוב ושוב גרסאות ישנות.
`hardReload()` מנקה `caches` וטוען מחדש עם `?v=<timestamp>`. כפתור בהדר ובהגדרות.
**`APP_VERSION` (1446) מוצג בהגדרות — לעדכן אותו בכל דחיפה**, זה כלי האבחון היחיד שיש ליעקב
כדי לדעת אם הוא מריץ את הגרסה שדחפתי.

⚠ בנייה של GitHub Pages ארכה 3+ דקות ב-18.8 (במקום דקה). לפני שמכריזים "לא עובד" —
לבדוק `GET /repos/:owner/:repo/pages/builds/latest` ולוודא `status: built`.

## ⚠ באגים ידועים ומלכודות (אומתו בקוד)

לפני שמציעים פיצ'ר — לבדוק אם מה שיעקב מתאר הוא אחד מאלה. מסודר לפי חומרה:

0. **🔴 נתוני המתפללים חשופים לחלוטין.** ה-Web App פרוס כ"כל אחד", וכתובתו מוטמעת ב-`index.html`
   שנמצא ב**ריפו ציבורי**. כל מי שיפתח `<webhook>?action=getMembers` יקבל את כל השמות, הטלפונים
   ויתרות החוב. עם הודעות שבת זה לא הזיק; עם חובות של אנשים זה חמור. **הוצג ליעקב 18.8.2026
   והוא טרם החליט.** התיקון המוצע: סוד שנשמר ב-`localStorage` בלבד ונשלח כפרמטר, ו-
   `handleMembersAction` מסרב בלעדיו. לא ליזום בלי אישור, אבל **להעלות שוב אם מתווסף מידע רגיש**.
1. **הזמנים באתר שונים מהזמנים שנשלחים אוטומטית.** ⚠ **יעקב מודע ובחר להשאיר כרגע** (13.8.2026) —
   לא לתקן בלי לשאול שוב, ורצוי לחזור לזה כשלוח עתים לבינה יגיע. הבקאנד וטאב "הודעה חופשית" משתמשים
   ב-`elevation=900&ue=on` ומחסירים דקה מהשקיעה; טאב **"זמני שבת" (שורה 1147) לא**. גובה 900 מ'
   מאחר את השקיעה בכ-4–5 דקות, ולכן ההודעה שיעקב רואה ושולח ידנית עלולה להיות **מוקדמת בכ-3–4
   דקות** מזו שנשלחת בטריגר. זה הפער החמור ביותר במערכת. התיקון: להשוות את שורה 1147 לשורה 1592
   (להוסיף `&elevation=900&ue=on`) ולהוסיף `subMins(shkia,1)` — כך שלושת מקומות החישוב יסכימו.
2. **הטוגל test/prod לא מגן על השליחה האוטומטית.** `setMode` רק כותב ללוג; `sendWeeklyZmanim`
   שולח תמיד ל-`CONFIG.EMAIL_TO`. יעקב עלול לחשוב שהוא "במצב בדיקה" בזמן שהטריגר שולח לכל הישוב.
   תיקון אמיתי: לשמור מצב ב-`PropertiesService` ולקרוא ממנו גם ב-`sendWeeklyZmanim`.
3. **חסר `i=on` בקריאות ל-Hebcal בבקאנד** (תוקן בפרונט 13.8.2026; ב-`Code.gs` עדיין חסר
   ב-`getParasha` שתי השיטות, ב-`checkIfHoliday` וב-`doGet`/`send`). ברירת המחדל של Hebcal
   היא **לוח חו"ל**. שתי תוצאות: (א) בשנים שבהן יו"ט אחרון של פסח חל בשבת, לוח הפרשות בארץ מקדים
   את חו"ל למשך שבועות — והמערכת תכריז על **פרשה שגויה**; (ב) יום שני של יו"ט (שהוא חול בארץ)
   ייחשב חג ותידלג שליחה שלא לצורך. **זה הבאג ההלכתי האמיתי כאן** — להוסיף `&i=on` לכל קריאות
   ה-`hebcal`/`shabbat`.
4. **"נשלח בהצלחה" הוא שקר.** `mode:'no-cors'` (1336) מונע קריאת התשובה — ההודעה הירוקה מוצגת גם
   כשהבקאנד החזיר `ok:false` או קרס. שיפור: JSONP לאישור, כמו ב-`getData`.
5. **פענוח כפול של פרמטרים.** Apps Script כבר מפענח את ה-query, והקוד קורא שוב
   `decodeURIComponent` על `body`/`subject`/`entry`/`templates`. הודעה שמכילה `%` (למשל "50% הנחה")
   תגרום ל-`URIError` → `ok:false` → ובגלל באג 4 המשתמש יראה "נשלח בהצלחה". להסיר את הפענוח הכפול.
6. **הבלאנק מוטמע כ-data URI.** Gmail חוסם `<img src="data:image/jpeg;base64,...">`. סביר שהלוגו
   והפוטר **אינם מוצגים אצל רוב הנמענים** — כדאי לבקש מיעקב לפתוח מייל אחרון ולוודא. הפתרון
   הנתמך ב-GmailApp: להעביר את התמונות ב-`inlineImages` ולהפנות אליהן ב-`src="cid:top"`.
7. **מספרי הדקות כתובים ידנית בשלושה מקומות** — `CONFIG`, המחרוזות ב-`buildPlainMessage`,
   והמחרוזות ב-`buildHtmlEmail`. ובפרונט: `STATE.settings`, `renderZmanim` (1224),
   `buildShabbatMessage` (1258). שינוי הפרש בלי לעדכן את כולם ⇒ הודעה שסותרת את עצמה.
8. **`Session.getActiveUser().getEmail()`** — משמש להתראות שגיאה, ל-`notifyGabbai` ולמייל
   הוואטסאפ. בטריגרים מתוזמנים הוא עלול להחזיר מחרוזת ריקה, וההתראה תיעלם בשקט. עדיף
   `getEffectiveUser()` או כתובת קשיחה.
9. **`action=zmanim` מסוכן** — שולח לפרודקשן בלי קשר ל-`mode`. הפרונט לא קורא לו כרגע, אבל מי
   שמכיר את ה-URL יכול. שווה למחוק או להצמיד ל-`mode`.
10. **הגיליון גדל ללא הגבלה.** `syncSaveHistory` תמיד מוסיף שורה, ו-`syncGetData` מחזיר את **כל**
    הגיליון בכל טעינת עמוד. עם הזמן ה-JSONP יאט משמעותית. כדאי להגביל ל-100 האחרונות.
11. **`event.currentTarget` ללא פרמטר `event`** — `switchTab` (1464), `loadZmanim` (1519),
    `fetchCustomZmanim` (1587). עובד ב-Chrome, נשבר בפיירפוקס ובכל קריאה תוכנתית.
12. **GET עם גוף ההודעה ב-URL** — הודעת חג ארוכה עלולה לחרוג ממגבלת אורך ה-URL ולהיכשל בשקט.
    מעבר ל-POST דורש `doPost` בבקאנד.
13. **`buildPlainMessage` משתמש בכוכביות** (`*14:30*`) — נכון לוואטסאפ, אבל זה גם גוף ה-plain של
    המייל, כך שמי שרואה טקסט בלבד רואה כוכביות.
14. **`doSend` מחליף `[פרשה]` פעם אחת** (`replace` ללא `g`, שורה 1334). הבקאנד עושה אותו דבר.
15. **סטטיסטיקות מזויפות בלוח הבקרה:** "50 נמענים" ו-"ה'" מוטמעים ב-HTML. ⚠ עכשיו יש מספר
    אמיתי לידם — `STATE.members.length` — והפער בין "50 נמענים" ל-31 מתפללים בולט וצריך תיקון.
16. **`syncLoad()` (1970) קוד מת**, וכן `getHebrewDate()` בבקאנד.
17. **`getParasha` שיטה 1 מתעלמת מפרמטר התאריך** — `shabbat` endpoint תמיד מחזיר את השבת הקרובה
    לרגע הריצה. תקין לטריגר של יום חמישי, שגוי אם מריצים ידנית בשבת עצמה.
18. **אין `health.html`** לפרויקט הזה, בניגוד לשני האחרים.
19. **הגיליון `RamchalSync` הוא כעת מסד הנתונים היחיד** — חמש לשוניות: `history`, `templates`,
    `members`, `ledger`, `hh`. הוא מאותר לפי **שם** דרך `getFilesByName`; שינוי שם או מחיקה
    יוצרים גיליון חדש וריק, והנתונים "נעלמים" (הם עדיין בסל המיחזור). אין גיבוי אוטומטי,
    בניגוד לפרויקט הצימר. **שווה להציע ליעקב גיבוי יומי לדרייב** — עכשיו יש מה לאבד.
20. **מחיקת מתפלל היא בלתי הפיכה** ומוחקת גם את היסטוריית החובות שלו (`membersDelete`).
    אין "לא פעיל" בממשק, למרות ששדה `active` קיים במודל ומכובד ב-`applyBulkCharge`.
21. **`hhDefaultYear()` מוטמע** ותקף עד תש"צ (2030). מעבר לכך יחזיר תמיד את הערך האחרון.
22. **מיזוג הסנכרון הוא "השרת גובר" ללא חותמות זמן.** שתי עריכות מקבילות משני מכשירים —
    האחרונה שנכתבה לגיליון מנצחת, בלי אזהרה. עם משתמש יחיד זה בסדר; עם גבאי שני זה יתפוצץ.

## מתכונים למשימות נפוצות

**שינוי הפרש זמן (למשל מנחה ל-25 דקות)** — שישה מקומות, תמיד:
בפרונט `#setting-mincha` (~933), ברירת המחדל ב-`STATE.settings` (1080), המחרוזת ב-`renderZmanim`
(1224) והמחרוזת ב-`buildShabbatMessage` (1258); בבקאנד `CONFIG` והמחרוזות ב-`buildPlainMessage`
וב-`buildHtmlEmail`. **משתמש קיים לא יראה שינוי בברירת המחדל** — ה-localStorage שלו גובר; להזכיר
לו לעדכן בטאב ההגדרות או לאפס.

**שינוי נוסח מייל השבת** — שני נוסחים נפרדים: `buildShabbatMessage` (ידני מהאתר) ו-
`buildPlainMessage`+`buildHtmlEmail` (אוטומטי). לוודא שהם לא מתפצלים.

**הוספת תבנית מובנית** — מפתח ל-`templates` ב-`loadTemplate` (1440) + כפתור (~839). אם היא אמורה
להתמלא בזמני שבת: להוסיף לתנאי בשורה 1438 ולוודא שהתוויות תואמות **בדיוק** ל-`fieldsMap` (1661),
כולל גרשיים ורווחים.

**"המייל השבועי לא הגיע"** — סדר בדיקה: (א) האם הייתה שבת עם חג בשישי/ראשון? זה **דילוג מכוון**,
ואמור היה להישלח מייל "יש לשלוח ידנית" — אם גם הוא לא הגיע, ראו באג 8; (ב) Executions בעורך
Apps Script — יש ריצה ביום חמישי? (ג) האם הטריגר קיים בכלל (`setupWeeklyTrigger` נמחק בטעות?);
(ד) מכסת Gmail היומית (חשבון רגיל: 100 נמענים/יום); (ה) Hebcal החזיר שגיאה → נשלח מייל שגיאה.

**"שליחה ידנית מהאתר לא עובדת"** — (א) מצב test/prod; (ב) "בדוק חיבור" בהגדרות; (ג) האם ה-URL
של ה-deployment השתנה (משתנה בכל deployment חדש → לעדכן בשורה 1079 **וגם** בהגדרות אצל המשתמש,
כי localStorage גובר); (ד) באג 5 — יש `%` בהודעה?

**דחיפה לגיטהאב** — קובץ יחיד, `main`, `index.html`. **לעדכן `APP_VERSION` בכל דחיפה.**
GitHub Pages מתעדכן תוך דקה-שלוש; אם יעקב לא רואה שינוי — לבדוק `pages/builds/latest` לפני
שמאשימים את המטמון, ורק אחר כך להפנות לכפתור "רענון עמוק".

**הוספת שדה למתפלל** — ארבעה מקומות: המודאל ב-HTML (`#mf-*`), `openMemberModal` ו-`saveMemberForm`
בפרונט, `SEED_MEMBERS` אם יש ערך התחלתי, ובבקאנד **גם** שורת הכותרות ב-`getMembersSheets`,
**גם** `membersGetAll` **וגם** `membersUpsert`. הוספת עמודה באמצע תשבש גיליון קיים — להוסיף בסוף.

**"החובות נעלמו"** — סדר בדיקה: (א) האם זה מכשיר אחר / דפדפן אחר (localStorage נפרד)?
(ב) האם הגיליון `RamchalSync` קיים בשם הזה ויש בו לשונית `members` עם שורות?
(ג) האם ה-webhook בהגדרות תקין (`?action=getMembers` בדפדפן)? (ד) האם נמחקו נתוני גלישה?
הגיליון הוא מקור האמת — אם הוא שלם, "סנכרן לשרת" מהטאב יחזיר את המצב.

## הקשר רחב

הפרויקט הוא חלק ממשפחה של שלושה ריפואים תחת `shirat-hatziporim`, כולם של יעקב גרזון ממיצד:
`metzad-website` (אתר הישוב), `shirat-hatziporim` (מערכת ההזמנות לצימר), ו-`ramchal-messages` (כאן).
הקונבנציות זהות: HTML יחיד, Apps Script כבקאנד, JSONP, ועברית בכל הממשק. יש סקילים נפרדים לשניים
האחרים — אם השאלה נוגעת לאתר הישוב או לצימר, זה הסקיל הלא נכון.

**⚠ שינוי אופי הפרויקט (18.8.2026):** מה שהיה "מערכת שליחת הודעות" מחזיק כעת גם **נתונים אישיים
של 31 משפחות** — שמות, טלפונים, וחובות כספיים. זה מעלה את רף הזהירות: גיבוי, פרטיות (באג 0),
ומחיקות בלתי הפיכות (באג 20) הם עכשיו שיקולים אמיתיים ולא תיאורטיים.
